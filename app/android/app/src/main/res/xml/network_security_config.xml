<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks plain-HTTP (cleartext) traffic by default since API 28.
  The backend has no TLS yet, so cleartext is allowed only for the dev
  hosts the app actually talks to: 10.0.2.2 is the Android emulator's
  alias for the host machine's localhost. Add the LAN/Tailscale hostname
  here once the backend moves off localhost, and drop this file entirely
  once the backend serves HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
